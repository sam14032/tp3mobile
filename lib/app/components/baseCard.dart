import 'package:flutter/material.dart';

class baseCard extends StatelessWidget {

  String letter;
  baseCard(String letter) {
    this.letter = letter;
  }

  @override
  Widget build(BuildContext context) {
    return Card(

    );
  }
}
